RefMatch 0.4.1 - StreamMode runtime candidate

Start with INSTALL-DA.txt.
This is the complete SOURCE package, not a compiled plugin.
Replace both the source folder and the repository-root GitHub workflow.
The helper app is built and embedded automatically; it owns Spotify Automation
permission. Logic's screen/system audio capture permission remains separate.

Build and Logic-runtime verification are still required. See BUILD-NOTES.txt.
